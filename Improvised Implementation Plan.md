OCEANA — Implementation Plan: Improvements & Gaps
-------------------------------------------------

### WHAT ANTIGRAVITY GOT RIGHT

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   ✓ Whisper → Statement → Echo principle captured  ✓ Font size escalation on philosophy lines  ✓ Scroll indicator replacement  ✓ "Enter Oceana" elevation  ✓ Question/Answer scale ratio  ✓ Button border-radius correction to 3px  ✓ Closing line opacity escalation  ✓ Arabic labels on tradition cards  ✓ Culture-specific overlay gradients  ✓ Peak moment 56px gap  ✓ Shimmer on primary CTA   `

### MISSING — ADD THESE

#### 1\. SCROLL-MARGIN-TOP — Critical Bug Not Addressed

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The navigation anchor offset bug   (menu scrolls to halfway) is not   mentioned anywhere in this plan.  ADD TO PLAN:    Every section with an id anchor     must have:    scroll-margin-top: 72px;    Affected sections in Story.tsx:    - Hero section    - Legacy section      - Traditions section    - Timeline section    - Matters section    - Reviews section (when added)    This is a launch blocker.    It must be in the plan.   `

#### 2\. FONT LOADING — Not Addressed

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`The plan implements Cinzel and   Cormorant Garamond treatments   throughout but says nothing about   ensuring these fonts load correctly.  ADD TO PLAN:    In index.html :`    

      `href="/fonts/cormorant-garamond-italic.woff2"      crossorigin>`

      `href="/fonts/cinzel-regular.woff2"      crossorigin>    In CSS/Tailwind config:    font-display: swap on all     @font-face declarations.    WHY CRITICAL:    Without this, on slow Saudi 4G     connections, the entire page     renders in system fonts for     2-4 seconds. The design     completely collapses.`

#### 3\. NOTO NASKH ARABIC — Rendering Verification Missing

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The plan adds Arabic text throughout   but doesn't address the font   rendering issue visible in the   current screenshots.  The Arabic is currently rendering   in a fallback serif — not Noto   Naskh Arabic as specified.  ADD TO PLAN:    Verify Noto Naskh Arabic is:    1. Correctly imported in index.html    2. Applied via font-family in the        relevant className    3. Weights 400, 500, 600 loaded    The specific Google Fonts URL needed:    https://fonts.googleapis.com/css2?    family=Noto+Naskh+Arabic:wght@400;500;600    &display=swap    All Arabic elements must use:    font-family: 'Noto Naskh Arabic', serif    direction: rtl    lang="ar" attribute on the element    Test specifically on:    - Samsung Galaxy (most common in KSA)    - iPhone with Safari    Arabic can render differently     across these platforms.   `

#### 4\. IMAGE REPLACEMENT STRATEGY — Incomplete

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The plan says:  "I will replace the current images   with high-quality, dramatic Unsplash   images that accurately reflect the   cultures."  This is insufficient direction.  IMPROVE TO:    Heart of Arabia — specific requirements:      Must show: recognisably Arabian food      Acceptable: Kabsa, Ouzi, Arabic mezze,                  Harees, Mutabbaq      NOT acceptable: Any curry, naan,                       or Indian-origin dish      Unsplash search: "arabic food dark"                       "kabsa rice lamb"                       "middle eastern mezze dark"      Minimum resolution: 1200×800px      Must survive: rgba overlay at 65% opacity      Color temperature: warm dark burgundy    Soul of China — specific requirements:      Must show: clearly Chinese cuisine      Acceptable: Dim sum, Peking duck,                  wok flames, hand-pulled                   noodles, har gow      NOT acceptable: Sweet and sour with                       broccoli, generic                       takeaway presentation      Unsplash search: "chinese food dark dramatic"                       "dim sum dark moody"                       "wok flames restaurant"      Minimum resolution: 1200×800px      Color temperature: deep jade/forest    INTERIM FALLBACK:    If suitable Unsplash images cannot     be found, use CSS gradients:    Arabia: background: linear-gradient(      135deg, #2A0810 0%, #180408 100%)    China: background: linear-gradient(      135deg, #051A10 0%, #030F08 100%)    Better to have atmospheric CSS     than a wrong photograph.    DO NOT use a placeholder that     misrepresents the cuisine.   `

#### 5\. LUCIDE ICONS — Wrong Choice for China

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The plan specifies:  "UtensilsCrossed (chopsticks) from   lucide-react library"  UtensilsCrossed is a fork and knife   crossed — NOT chopsticks.  There is no chopstick icon in   lucide-react.  CORRECT SOLUTION:    Option A — Custom inline SVG:    const ChopstickIcon = () => (   `

             `viewBox="0 0 20 20"            fill="none">`

              `stroke="#C9A84C" stroke-width="1.5"          stroke-linecap="round"/>`

              `stroke="#C9A84C" stroke-width="1.5"          stroke-linecap="round"/>    )    Option B — Use lucide Minus × 2     positioned diagonally via CSS transform.    Option C — Use the existing     fork icon but rotate it 45deg     and duplicate — quick hack     but visually reads as chopsticks.    RECOMMENDATION: Option A.    10 lines of JSX. Zero dependencies.    Perfectly on-brand.    Also — Flower2 for India:    This is acceptable as a lotus     substitute BUT verify it renders     at the correct stroke weight.    At sizes below 20px Flower2     can become unrecognisable.    Consider: Sparkles icon instead —     reads as spice/magic at small sizes.`

#### 6\. WAVE ANIMATION — Performance Risk

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The plan adds:  "subtle SVG wave pattern at bottom   of hero with 12s infinite horizontal   drift animation"  This is correct in direction but   needs performance constraints.  ADD TO PLAN:    The wave must use:    transform: translateX() ONLY    — never animate width, height,       left, top, or opacity on the       wave element directly    will-change: transform on the     animated wave element    Remove will-change after component     unmounts (React cleanup):    useEffect(() => {      const wave = waveRef.current;      if (wave) wave.style.willChange = 'transform';      return () => {        if (wave) wave.style.willChange = 'auto';      };    }, []);    Reduce motion support:    @media (prefers-reduced-motion: reduce) {      .hero-wave {        animation: none;      }    }    WHY: Without these constraints,     the wave animation will cause     frame drops on mid-range Android     devices — which represent a     significant portion of Saudi     mobile users.   `

#### 7\. FORMATOCEANATEXT — Needs Specification

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The plan references:  "tweaking the formatOceanaText   implementation to remove font-bold"  This is vague. Here is the   exact specification:  ADD TO PLAN:    formatOceanaText should:    1. Find the word "Oceana" in any string    2. Wrap it in a  with:       - color: #C9A84C       - font-weight: 400 (NOT bold)       - font-style: inherit (match parent)    3. NOT apply to: headings in Cinzel caps       (it looks wrong in all-caps contexts)    4. Apply to: body paragraphs,        closing lines, taglines only    Implementation:    const formatOceanaText = (text: string) => {      const parts = text.split(/(Oceana)/g);      return parts.map((part, i) =>        part === 'Oceana' ? (   `

                  `key={i}            style={{               color: '#C9A84C',              fontWeight: 400            }}          >            {part}        ) : part      );    };    IMPORTANT: This function should     NOT be applied to Arabic text —     the Arabic name الأوقيانا should     be styled as a whole unit,     not word-searched for "Oceana".`

#### 8\. MOBILE VIEWPORT — Not Tested in Plan

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The verification plan says:  "Verify the page rendering across   sections" and "desktop and mobile   viewports"  This is too vague for a page   with this many typographic   breakpoints.  REPLACE WITH:    Specific viewport test matrix:    320px  — iPhone SE (smallest target)             Check: "They haven't left."              doesn't overflow on one line             Check: Cinzel kicker doesn't              overflow with 0.32em tracking    390px  — iPhone 15 Pro (primary target)             Full visual QA on every section    414px  — iPhone Plus / large Android             Check tradition cards              stack correctly    768px  — iPad / large phone landscape             Check the transition from              mobile to tablet layout    1024px — Desktop minimum             Check two-column timeline    Specific checks on 320px:    — Reduce letter-spacing on Cinzel       kickers to 0.15em at this breakpoint    — "They haven't left." may need       to be 38px not 44px minimum    — Tradition cards aspect-ratio       must be preserved   `

#### 9\. REVIEWS SECTION — Completely Missing

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The implementation plan covers   Story.tsx sections but makes no   mention of integrating the   Reviews section.  The Reviews section was fully   specified with:  - Five curated reviews  - Expand/collapse interaction  - Wave background  - "THIRTY YEARS OF TABLES" heading  - Gold floating card treatment  ADD TO PLAN:    Reviews section integration:    Position: After the timeline,    before the closing CTA section.    New component: ReviewsSection.tsx    Import and render in Story.tsx:    The full specification exists     in the reviews prompt MD file     already delivered.    This section is currently     absent from the live site —    it must be in the Story.tsx     implementation plan.   `

#### 10\. TRANSITION BETWEEN DARK/LIGHT SECTIONS

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   Not addressed in the plan at all.  The page alternates:  Dark → Light (ivory) → Dark →   Light → Navy → Light → Dark  The transitions between these   sections currently feel abrupt —   like hard cuts.  ADD TO PLAN:    Add a subtle bleed at section     boundaries using CSS:    /* Dark to Light transition */    .section-dark + .section-light::before {      content: '';      display: block;      height: 60px;      margin-top: -60px;      background: linear-gradient(        to bottom,        rgba(10,8,6,0) 0%,        var(--ivory) 100%      );      pointer-events: none;    }    /* Light to Dark transition */    .section-light + .section-dark::before {      content: '';      display: block;      height: 60px;      margin-top: -60px;      background: linear-gradient(        to bottom,        var(--ivory) 0%,        rgba(10,8,6,0) 100%      );      pointer-events: none;    }    WHY: This creates the feeling     of the page breathing —     sections dissolve into each other     rather than colliding.    It's a 10-line CSS addition     with enormous visual impact.   `

#### 11\. CTA SHIMMER ANIMATION — Needs EXACT SPEC

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The plan says:  "shimmer effect animation on   primary CTA button"  Without the exact spec this   will likely be implemented   incorrectly — too fast,   too bright, or looping   when it should fire once.  EXACT SPECIFICATION:    @keyframes shimmerSweep {      0%   {         transform: translateX(-100%);         opacity: 0;      }      20%  { opacity: 1; }      80%  { opacity: 1; }      100% {         transform: translateX(100%);         opacity: 0;      }    }    .btn-primary::after {      content: '';      position: absolute;      top: 0; bottom: 0;      width: 40%;      background: linear-gradient(        to right,        transparent 0%,        rgba(255,255,255,0.07) 50%,        transparent 100%      );      animation: shimmerSweep 800ms ease                  1200ms 1 forwards;      /* Delay: 1200ms after page load         Duration: 800ms         Fires: ONCE only (iteration: 1)         Not on hover — on load only */    }    .btn-primary {      position: relative;      overflow: hidden; /* contains shimmer */    }    WHY ONCE NOT LOOP:    A looping shimmer on a CTA button     is visually aggressive and signals     urgency in a way that conflicts     with the brand's quiet confidence.    Once on load says: "we noticed you."    Looping says: "CLICK ME NOW."   `

#### 12\. OPEN QUESTIONS — NEED ANSWERS BEFORE BUILD

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   The plan lists open questions but   doesn't give a deadline for answers   or a fallback if no answer comes.  IMPROVE TO:    Q1: Unsplash URLs for Arabia/China?    FALLBACK IF NO ANSWER IN 24HRS:    Use CSS gradient atmospheres     as specified above.     Do not block build on this.    Q2: Any component library     constraints beyond lucide-react?    NEEDED BECAUSE:    The wave SVG and chopstick icon     are custom — need to know if     there's a restriction on     inline SVG in this codebase.    Q3: Is formatOceanaText a shared     utility or local to Story.tsx?    NEEDED BECAUSE:    If shared, the font-weight change     affects other pages that use it.    Q4: What is the current     scroll behavior CSS?    NEEDED BECAUSE:    The scroll-margin-top fix requires     knowing whether smooth scroll     is set on html element or     handled via JS.    Q5: Is the Reviews section     a separate component or should     it live in Story.tsx?    NEEDED BECAUSE:    File architecture affects     how the section is integrated.   `

### PRIORITY ORDER FOR ANTIGRAVITY

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   Implement in this exact order:  PHASE 1 — Critical (must ship):    1. scroll-margin-top on all sections    2. Font loading + display: swap    3. Noto Naskh Arabic font verification    4. Image replacements (Arabia + China)       or CSS fallbacks if not found  PHASE 2 — High Impact (most visible):    5. "They haven't left." scale contrast    6. "Enter Oceana." elevation    7. Question/Answer scale ratio    8. Closing line opacity escalation    9. Philosophy line size escalation    10. Remove all quotation marks  PHASE 3 — Polish:    11. Wave animation with         performance constraints    12. Shimmer on CTA (exact spec above)    13. Section transition bleeds    14. Chopstick SVG icon    15. Arabic label permanent visibility    16. formatOceanaText correction  PHASE 4 — Verification:    17. Test matrix on all 5 viewports    18. Reduced motion check    19. Arabic RTL on Samsung + iPhone    20. Reviews section integration   `

### THE ONE THING MISSING FROM THE ENTIRE PLAN

Plain textANTLR4BashCC#CSSCoffeeScriptCMakeDartDjangoDockerEJSErlangGitGoGraphQLGroovyHTMLJavaJavaScriptJSONJSXKotlinLaTeXLessLuaMakefileMarkdownMATLABMarkupObjective-CPerlPHPPowerShell.propertiesProtocol BuffersPythonRRubySass (Sass)Sass (Scss)SchemeSQLShellSwiftSVGTSXTypeScriptWebAssemblyYAMLXML`   There is no mention of the   hero background photograph.  The current implementation uses   a CSS gradient — which is correct   as an interim solution.  But the plan makes no provision for:    — When the real photograph arrives    — How it gets integrated    — What CSS treatment it receives    — How the overlay adjusts  ADD TO PLAN:    Hero image integration —     when Corniche photography is ready:    1. WebP format, 1920×1080px minimum    2. JPEG fallback    3. Preloaded in     4. Overlay adjusts from current        gradient-only to:       gradient + photograph layered    5. The wave animation sits above        the photograph layer,        not above the gradient    6. Image receives blur-up treatment        on slow connections    Until then: CSS gradient stays.    This must be explicitly stated     so no one ships the site with     an Italian lake photo because     "we needed something."   `